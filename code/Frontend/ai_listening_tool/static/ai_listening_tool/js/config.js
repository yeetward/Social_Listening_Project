(function () {
  window.RM_TOOL_CONFIG = {
    API_BASE: "http://127.0.0.1:8000/api", // keep the /api base, no trailing slash
    timeoutMs: 120000  // 2 minutes - generous timeout for AI processing
  };
})();
